# Programmers Level 2

### 131127 문제링크

[131127 - 할인 행사](https://school.programmers.co.kr/learn/courses/30/lessons/131127)

<br>

### Comment

- 나는 해시를 활용해서 했지만 다른 풀이에선 `slice`, `filter`등 배열 메소드를 활용해서 풀었다.
- 챗gpt한테 내 방법을 좀 더 최적화할 방법이 없냐 물어보니 슬라이딩 윈도우라는 걸 알려주었다.

- 난 가능한 모든 할인 조합을 만들어서 그걸 원하는 상품 개수 목록과 비교했는데, 이럴 경우 매 구간마다 10일을 새로 계산해서 `available` 객체를 계속 새로 만들기 때문에 비효율적이다.
- 챗gpt는 처음 10일만 계산하고, 10일 이후는 하루 빠지는 상품은 -1, 하루 새로 들어오는 상품은 +1을 해서 매번 전체를 다시 세지 않는다. (슬라이딩 윈도우) `solutionWithSlidingWindow` 함수 참고
- 연속된 구간 문제, 구간 크기가 고정적이거나 점진적으로 변할 경우, 이전 결과를 재활용할 수 있는 경우 슬라이딩 윈도우를 고려할 수 있다.
